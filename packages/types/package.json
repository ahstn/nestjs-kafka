{
  "name": "@nestjs-kafka/types",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "dev": "tsc -w",
    "lint": "TIMING=1 eslint src/**/*.ts* --fix"
  },
  "devDependencies": {
    "@nestjs-kafka/eslint": "*",
    "@nestjs-kafka/tsconfig": "*",
    "@types/jest": "^26.0.22",
    "eslint": "^7.32.0",
    "typescript": "^4.5.3"
  }
}
